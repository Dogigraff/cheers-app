# Быстрая проверка работы приложения

## Сервер запущен

✅ Dev сервер работает на **http://localhost:3001**

## Что проверить

1. **Откройте браузер:** http://localhost:3001

2. **Откройте DevTools (F12)** и проверьте:
   - Вкладка **Console** - нет ли ошибок
   - Вкладка **Network** - загружаются ли ресурсы Яндекс.Карт

3. **Что должно быть видно:**
   - Карта Яндекс.Карт (может быть темной из-за фильтров)
   - Кнопка 🔥 внизу по центру экрана
   - Если есть маяки - эмодзи-маркеры на карте

## Если карта черная

**Временное решение - отключить фильтры:**

Откройте `app/globals.css` и закомментируйте строки с фильтрами:

```css
/* Временно отключено для отладки
.yandex-map-dark [class*="ymaps-2"][class*="-map"] canvas {
  filter: grayscale(0.6) invert(1) contrast(1.1) brightness(0.9) !important;
}
*/
```

## Проверка консоли

В консоли браузера должно быть:
- ✅ "Yandex Map loaded successfully" - карта загрузилась
- ❌ Нет ошибок "Invalid API key"
- ❌ Нет ошибок загрузки ресурсов

## Следующие шаги

Если карта все еще не видна:
1. Проверьте консоль на ошибки
2. Попробуйте отключить CSS фильтры временно
3. Проверьте, что Яндекс.Карты загружаются в Network tab
