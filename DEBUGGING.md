# Отладка и проверка работы приложения

## ✅ Сервер запущен

Сервер разработки работает на `http://localhost:3000`

## 🔍 Пошаговая проверка

### 1. Откройте браузер
Перейдите на: **http://localhost:3000**

### 2. Откройте DevTools (F12)
Нажмите `F12` для открытия инструментов разработчика.

### 3. Настройте геолокацию (важно!)

**Способ А: Через Sensors (рекомендуется)**
1. В DevTools нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
2. Введите `Sensors` и выберите `Show Sensors`
3. В открывшейся панели найдите `Location`
4. Выберите `Moscow` или введите координаты вручную:
   - Latitude: `55.763`
   - Longitude: `37.593`

**Способ Б: Разрешить реальную геолокацию**
- Если вы в Москве, просто разрешите доступ к геолокации
- Если нет - используйте Способ А

### 4. Обновите страницу
Нажмите `F5` или `Ctrl+R` для обновления страницы.

### 5. Проверьте консоль
В DevTools откройте вкладку `Console` и проверьте:
- ✅ Нет ошибок (красные сообщения)
- ✅ Видите запросы к Supabase RPC функции
- ✅ Данные загружаются успешно

## 🐛 Возможные проблемы

### Проблема: Карта пустая, нет маркеров

**Проверьте:**

1. **Консоль браузера:**
   ```javascript
   // Откройте Console и выполните:
   navigator.geolocation.getCurrentPosition(
     pos => console.log('Location:', pos.coords.latitude, pos.coords.longitude),
     err => console.error('Error:', err)
   );
   ```

2. **Проверьте переменные окружения:**
   - Убедитесь, что файл `.env.local` существует
   - Проверьте, что `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` установлены

3. **Проверьте данные в базе:**
   ```sql
   -- Выполните в Supabase SQL Editor:
   SELECT COUNT(*) FROM beacons WHERE is_active = TRUE AND expires_at > NOW();
   ```

4. **Проверьте RPC функцию:**
   ```sql
   -- Выполните в Supabase SQL Editor:
   SELECT * FROM get_nearby_beacons(55.763, 37.593, 5000);
   ```

### Проблема: Ошибка "Failed to fetch beacons"

**Возможные причины:**
1. Неправильные credentials в `.env.local`
2. RLS политики блокируют запрос
3. Нет активных маяков в базе

**Решение:**
1. Проверьте `.env.local` файл
2. Убедитесь, что RLS политики настроены правильно
3. Заполните базу тестовыми данными (см. `scripts/seed-with-users.sql`)

### Проблема: Leaflet карта не загружается

**Проверьте:**
1. CSS для Leaflet импортирован в `app/layout.tsx`
2. Нет конфликтов с другими библиотеками
3. Браузер поддерживает современный JavaScript

## 📊 Что должно работать

✅ Карта загружается (темная тема)
✅ Запрос геолокации появляется
✅ После получения координат загружаются маяки
✅ На карте видны эмодзи-маркеры (🍷, 🥃, 🍺)
✅ Клик на маркер открывает Sheet снизу
✅ В Sheet показываются детали маяка

## 🔧 Быстрая проверка через Network

1. Откройте DevTools → вкладка `Network`
2. Обновите страницу
3. Найдите запрос к `get_nearby_beacons`
4. Проверьте:
   - Статус: `200 OK`
   - Response содержит массив маяков

## 📝 Логи для отладки

Добавьте в консоль браузера:
```javascript
// Проверка хука
localStorage.setItem('debug', 'true');

// Проверка Supabase клиента
import { createClient } from '@/utils/supabase/client';
const supabase = createClient();
console.log('Supabase URL:', supabase.supabaseUrl);
```

## 🆘 Если ничего не помогает

1. Проверьте логи сервера в терминале
2. Убедитесь, что база данных заполнена тестовыми данными
3. Проверьте, что пользователи созданы в Supabase Auth
4. Убедитесь, что RLS политики разрешают чтение маяков
