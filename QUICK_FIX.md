# Быстрое решение проблемы с портом 3000

## Проблема
На `localhost:3000` открывается другой сайт (мемориальный сайт), а не наше приложение Cheers.

## Решение

### Вариант 1: Использовать порт 3001 (уже запущено)

✅ **Сервер уже запущен на порту 3001**

Откройте в браузере:
👉 **http://localhost:3001**

### Вариант 2: Остановить процесс на порту 3000

Если хотите использовать порт 3000:

1. Найдите процесс, занимающий порт:
   ```powershell
   netstat -ano | findstr :3000
   ```

2. Остановите процесс (замените PID на реальный):
   ```powershell
   taskkill /F /PID <PID>
   ```

3. Запустите наш сервер:
   ```powershell
   cd c:\Users\user\Downloads\cheers-app
   npm run dev
   ```

## Проверка работы

1. Откройте **http://localhost:3001** в браузере
2. Откройте DevTools (F12)
3. Настройте геолокацию через Sensors:
   - `Ctrl+Shift+P` → `Sensors` → `Show Sensors`
   - Выберите `Moscow` или координаты: `55.763, 37.593`
4. Обновите страницу (F5)

## Что должно быть видно

- Темная карта OpenStreetMap
- Эмодзи-маркеры (🍷, 🥃, 🍺) вместо стандартных булавок
- При клике на маркер открывается Sheet снизу

## Если карта пустая

1. Проверьте консоль браузера (F12 → Console) на ошибки
2. Убедитесь, что файл `.env.local` существует с правильными credentials
3. Проверьте, что в базе есть активные маяки
